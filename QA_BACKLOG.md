# QA_BACKLOG.md

> TestFlight Build 1.0(2) 피드백 기준 QA 추적. 작성일: 2026-06-12
> 모든 수정은 `index.html` 워킹트리(미커밋)에 존재.

---

## 1. 버그 목록 / 상태 / 우선순위

### ✅ 수정 완료

| ID | 우선순위 | 증상 | 원인 | 수정 |
|---|---|---|---|---|
| BUG-01 | P0 | 타임아웃 자동선택 시 실제로는 승/패인데 **무승부로 표시** | 호스트 `autoFillChoices`가 로컬 stale state 기준으로 **이미 제출된 선택을 랜덤값으로 덮어씀**(전파 지연) → 판정이 랜덤값 기준 | `autoFillChoices`가 DB 재조회 후 빈 선택만 채움. 자동선택은 `isAutoSelected` 인코딩. 판정은 `judgeRound` 단일 함수, 호스트만 수행 |
| BUG-02 | P0 | 게임 중 "승률 보기"에 **현재 라운드만** 표시 | `showStatsPopup`이 `state.participants`(현재 라운드)만 사용 | `buildRoomStatsSummary(roomCode, live)`로 같은 방의 보관 라운드 + 현재 라운드 합산(roomCode 기준) |
| BUG-03 | P0 | 게임 종료 후 홈 → 직전 게임 결과 **기록 없음** | 직전 게임을 영구 저장하지 않음 | `endGame`에서 `rpsLastCompletedGame` 저장, 홈 버튼이 스냅샷 로드, 앱 재실행 후 유지, 저장 실패 토스트+재시도 |
| BUG-04 | P1 | 강제종료/프리즈 후 **새 방에 이전 게임 화면/상태 잔류** | createRoom/joinRoom이 라운드/판정/타이머/오버레이/저장본을 정리하지 않음 | `resetRoomLocalState()` 추가·호출(같은 방 재접속 저장본은 보존) |
| BUG-05 | P1 | **3인 게임에서 판정이 안 나오고 선택 화면에서 멈춤** | 호스트가 우선안전/술래로 라운드에서 빠지면 **선택 타이머가 없어 판정 백스톱 부재** | `startHostJudgeBackstop()`가 `getCountdownStartAt()+11s`에 판정 보장 |
| BUG-06 | P1 | 자동선택인지 직접 선택인지 사용자가 구분 불가 | 라벨 없음 | 결과 카드에 자동선택만 "자동/Auto/自動" 라벨(`tag.auto`) |
| BUG-07 | P1 | 게임 화면 하단 버튼/UI 잘림 (iPhone) | 100vh 사용 + safe-area 미반영 | body/.app `100dvh`, footer/popup `env(safe-area-inset-bottom)` |
| BUG-08 | P2 | 호스트가 "게임준비"로 표시(추정) | — | 검증 결과 `getParticipantBadge`가 호스트 우선 판별 → "👑 호스트" 이미 정상. 호스트는 준비 버튼 비노출 |
| BUG-09 | P2 | 결과 문구 "생존"/"승리" 혼재 | — | 최종 승자 문구 **승리!/Win!/勝利！**로 통일. "승래" 오타는 존재하지 않음(확인) |
| BUG-10 | P2 | 참가자 카드 정보 과다 | — | 라운드 결과 목록을 1열 풀폭, 이름 굵게 + 선택 아이콘·자동·결과 한 줄 |
| BUG-11 | P2 | Me/방코드/벌칙 레이블 과대 | — | `.compact-info-row .label` 12px/700 축소, 값(코드 16px·벌칙 17px) 유지 |
| BUG-12 | P2 | 역할별 상태 문구 혼란 | — | 하드코딩 한글(게임 대기/우선안전/재대결 시작/대기 중) → `ready.*` i18n 키 |
| BUG-13 | P2 | 영어 UI 문구 점검 | — | 게임/역할 문구 i18n화로 영문 모드 정리(토스트류는 미정리 잔존) |

### ⚠️ 미해결 / 미검증

| ID | 우선순위 | 내용 |
|---|---|---|
| OPEN-01 | P1 | **BUG-05 실기기 재현 검증 미완** — 런타임 동작이라 단위 테스트 불가. 호스트가 빠지는 재대결 멀티디바이스 테스트 필요 |
| OPEN-02 | P2 | 다수 토스트 메시지가 한글 하드코딩(영문/일문 모드에서 한글 노출). 예: "방 코드를 입력해주세요" 등 |
| OPEN-03 | P2 | P2.10/11 UI는 코드상 적용했으나 **실기기 시각 확인 미완** |
| OPEN-04 | P3 | choice 권장 구조의 `roundId`/`selectedAt`는 미도입(Supabase 컬럼 추가 필요) — 현재는 choice 인코딩에 isAuto만 |

---

## 2. 회귀 테스트 시나리오 (Build 3 업로드 전 필수)

1. 2인 게임 직접선택 → 승/패 정확
2. 2인 게임 자동선택 포함 → 자동선택 측 승/패 정확 + "자동" 라벨
3. 3인 게임 술래 1명 → 정확 판정
4. **3인 게임 술래 2명 → 판정 멈춤 없음 (BUG-05 핵심)**
5. 3종류 선택 → 전원 무승부
6. 같은 방 5라운드+ 진행 후 승률 보기 → 전체 누적 표시
7. 게임 종료 후 홈 → 직전 게임 결과 표시
8. 앱 강제종료 후 재실행 → 직전 결과 유지
9. 새 방 생성/입장 → 이전 방 상태 잔류 없음 (BUG-04)
10. 작은 화면 iPhone(SE/mini) → 하단 UI 잘림 없음
11. (추가) **호스트가 우선안전으로 빠진 재대결**에서 라운드 자동 판정되는지 (BUG-05)
12. (추가) KO/EN/JA 3개 로케일에서 결과/라벨/문구 점검

---

## 3. 검증 완료된 항목
- 판정/인코딩 로직 단위 테스트 통과(2인/3인, 2종류/3종류, 자동선택 혼합, 하위호환).
- `npm test`(문법), `npm run build`, `npx cap sync ios` 통과.
- QRScannerPlugin.swift / signing / pbxproj 미변경 확인.
