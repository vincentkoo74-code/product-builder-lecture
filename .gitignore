.claude/
.gstack/
mockups/
tmp/
# 원본 zip 폴더 (정리본은 ASSETS/fonts/ 에 둠)
ASSETS/font/
# Apple Sign In private key (절대 commit 금지)
*.p8
AuthKey_*.p8
# Node.js
node_modules/
package-lock.json
dist/
.derivedData*/
.DS_Store
android/.gradle/
android/**/build/
android/local.properties
android/.idea/
.asc-upload.env
.env.local
asc-upload.env
/build/
# Build21: 음성 재생성 스크립트가 실행 전마다 만드는 백업(재생성 가능, git 미추적으로 저장소 비대화 방지)
ASSETS/voice-backups/

# 실기기 QA export 원본(JSON) — 저장소 밖에서 관리, 요약은 docs/history/에 기록
QA-index/

# V1.0_JP: Supabase CLI 로컬 링크 상태(supabase link 가 생성). 국가별 브랜치에서
# 서로 다른 project 를 가리키므로 추적하면 브랜치 전환 시 잘못된 프로젝트로 링크된
# 상태가 상속된다 — 오배포 경로이므로 git 추적하지 않는다.
supabase/.temp/

# Playwright E2E 산출물
/test-results/
/playwright-report/

# 로컬 E2E 런타임(로컬 전용 비밀 포함) — 절대 커밋하지 않는다
/.jp-e2e/
