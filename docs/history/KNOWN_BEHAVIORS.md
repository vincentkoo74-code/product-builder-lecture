# 🟢 KNOWN BEHAVIORS — 버그가 아닌 정상 동작

> QA 과정에서 **반복적으로 버그로 오인되는 정상(의도된) 동작**을 기록한다.
> 새 항목 제보 시: "이거 버그 아닌가?" → 여기 있으면 정상. 없으면 `BUG_MASTER_LEDGER.md`로 등록 검토.
> 갱신: 2026-06-22 · 기준 코드: `35cd68d`(build8)

---

| # | 항목 | 설명 (왜 정상인가) | 최초 도입 Build | 현재 상태 |
|---|---|---|---|---|
| KB-01 | 마지막 활성 참가자가 Ready → **자동 시작** | 활성(비호스트) 전원 ready 시 호스트 권위로 자동 게임 시작. 호스트가 버튼을 안 눌러도 시작되는 건 **Build4 정식 사양**. | Build4 | 정상 |
| KB-02 | 자동 선택은 결과에 **"자동/Auto/自動"** 표시 | 5초 타임아웃 시 호스트가 빈 선택만 랜덤 채움. 직접 선택과 구분 위해 라벨 부착(직접선택엔 라벨 없음). | Build3 | 정상 |
| KB-03 | 호스트 이탈 시 **자동 승계** | 호스트가 나가면 다음 호스트 지정/랜덤 승계로 게임 지속. "방장이 바뀌었다"는 정상. | Build2 | 정상 |
| KB-04 | 재게임 초대 흐름 | 종료 후 `reinviting` → 참가자에 초대 팝업 → 카운트다운 → `ready` 전환. 팝업이 자동으로 닫히고 준비화면 진입이 정상. | Build2 | 정상 (단 WRPS-013 예외 케이스 주의) |
| KB-05 | 무승부 = **1종류 또는 3종류** 선택 | 활성 전원이 같은 선택(1종류)이거나 가위·바위·보 3종류 모두 나오면 전원 무승부. 정확히 2종류일 때만 승/패. "3명이 다 다른 거 냈는데 왜 무승부?" → 정상. | Build1 | 정상 |
| KB-06 | 술래 수 상한 = **비호스트 − 1** | N명(비호스트)일 때 술래는 최대 N−1명. 전원 술래(deadlock) 방지. 드롭다운에 1..(N−1)만 노출되는 건 정상. | Build4 | 정상 |
| KB-07 | 확정 술래/안전자는 재대결에서 **제외** | 술래/안전이 확정되면 다음 대결에 Ready/선택 불가, **대기화면 유지**가 정상(고착처럼 보이나 의도됨). | Build3/4 | 정상 |
| KB-08 | 상태가 가끔 **~3초 지연** 갱신 | Realtime이 불안정할 때 3초 폴링 백업이 보정. 즉시 반영 안 되고 잠깐 늦는 건 폴백 동작. | Build1/2 | 정상 |
| KB-09 | 게임 시작 직전 **"잠시 후 시작"** 잠깐 멈춤 | 카운트다운이 서버 예정시각(`serverNow`/`countdownStartAt`)까지 대기 후 동시 시작 → 기기 동기화를 위한 의도된 대기. | Build6 | 정상 |
| KB-10 | 기록 저장 방식(3중) | ① participants 누적 전적 ② localStorage 스냅샷(`rpsLastCompletedGame`/`rpsRoundStatsArchive`) ③ 로그인 시 Supabase. 오프라인에서도 직전 결과 유지가 정상. | Build3 | 정상 |
| KB-11 | 게임 결과 후 **자동 전환 없음** | 결과/통계 화면은 수동 버튼 클릭으로만 넘어감(타임아웃 자동전환 없음). "화면이 안 넘어간다" → 버튼 대기 중. | Build1/2 | 정상 |
| KB-12 | 빠진 호스트 **11초 백스톱** | 호스트가 우선안전/술래로 빠지면 선택 타이머가 없어 판정 트리거 부재 → `countdownStartAt+11s`에 호스트가 판정 보장. 약간의 지연 후 판정되는 건 정상. | Build3 | 정상 |

---

## ⚠️ 표현이 모호해 확인이 필요한 동작 (정상/버그 경계)

| 항목 | 코드 동작 | 문서 표현 | 비고 |
|---|---|---|---|
| 호스트의 게임 참여 | `runCountdownThenShowGame`(6382) 주석 "호스트도 가위바위보에 참여하는 일반 플레이어" — **호스트도 낸다** | `BUILD4/5_QA_SPEC`·`GAME_LOGIC` 일부: "호스트=심판, 가위바위보를 내지 않음" | 코드와 문서 표현 불일치. 실제 의도(호스트 참여 여부) **확정 필요** → 확정 후 KB 또는 WRPS로 분류 |

> 위 항목은 정상 동작으로 단정하지 말고, 제품 의사결정으로 확정한 뒤 `FEATURE_DECISION_HISTORY.md`에 기록할 것.

---

## 사용 규칙
1. QA/테스터 버그 제보 시 **먼저 이 문서 대조** → 오인 차단.
2. 새 "정상이지만 오인되는 동작" 발견 시 KB-NN 추가.
3. 정상 동작이 실제로 사양 변경되면 `FEATURE_DECISION_HISTORY.md`와 동기화.
