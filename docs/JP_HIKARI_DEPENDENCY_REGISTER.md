# MARU RPS V1.0_JP — HIKARI 의존성 등록부

최종 갱신: 2026-09-02 (Sprint JP-02B) · 관리: CRIS(엔지니어링) · 판단 주체: **HIKARI**

이 문서는 **엔지니어링이 답할 수 없는 질문**만 모은다.
CRIS 는 여기 있는 항목에 대해 코드로 답을 만들지 않는다 — 기술적 선택지와 결과만 기술한다.

---

## JP-BL-021 — 일본어 법무 문서 (출시 차단)

현재 상태: `legal/privacy.ja.html` · `legal/terms.ja.html` = **PENDING_HIKARI**
(상태 고지만 담고 있으며 법적 문언은 없다. 한국어판 번역을 승인된 일본어 문서로 쓰지 않았다.)

| # | 질문 |
|---|---|
| 1 | 일본어 개인정보 처리방침의 **필수 기재 항목**은 무엇인가 |
| 2 | 일본어 이용약관의 **필수 기재 항목**은 무엇인가 |
| 3 | **운영자 신원 필드** — 사업자명·대표자·주소 중 무엇을 표시해야 하는가 |
| 4 | **게시 위치** — 앱 내/외부 URL/LINE 심사 제출용 중 어디에 어떻게 게시하는가 |
| 5 | 외부送信(external transmission) 고지가 필요한가 — 현재 JP 런타임은 Google Fonts 로 나간다 |
| 6 | APPI 상 처리위탁·제3자 제공 고지 대상은 무엇인가(Supabase = Tokyo 리전) |

엔지니어링 준비 상태: 로케일 라우팅 완료. **문안만 주입하면 된다.** 한국어 유출 경로 0.

---

## JP-BL-022 — 계정 삭제 후 참가 기록 잔존 (법적 판단 필요 / 구현 보류)

**실측된 현재 동작** (로컬 스키마 검증, 2026-09-02):

| 테이블 | 계정 삭제 시 |
|---|---|
| `auth.users` | 삭제 |
| `user_game_stats` | **CASCADE 삭제** |
| `user_game_history` | **CASCADE 삭제** |
| `participants` | **잔존 — 닉네임 포함** |
| `rooms` | 잔존 |

| # | 질문 |
|---|---|
| 1 | 계정 삭제 후 `participants` 의 **닉네임 잔존**이 허용되는가 |
| 2 | 허용되지 않는다면 **익명화**와 **행 삭제** 중 무엇이 요구되는가 |
| 3 | 법령상 **보존 기간** 요구가 있는가 (있다면 얼마인가) |
| 4 | 삭제 완료를 사용자에게 어떻게 고지해야 하는가 |

**기술적 선택지와 결과** (CRIS 기술, 선택은 하지 않음):

| 안 | 구현 | 결과 | 위험 |
|---|---|---|---|
| A 현행 유지 | 변경 없음 | 닉네임이 방 기록에 남는다 | 법령 위반 가능성 — 판단 필요 |
| B 익명화 | 삭제 시 해당 참가자 행의 `name` 을 비식별 값으로 치환 | 게임 기록 구조 보존, 식별성 제거 | ⚠️ **대상 행을 특정할 수 없다**(아래) |
| C 행 삭제 | 참가자 행 삭제 | 완전 제거 | B 의 특정 문제 + 진행 중 라운드 판정 집합 변화(CORE 영향) |

> ⚠️ **2026-09-02 추가 확인 — B/C 의 전제가 성립하지 않는다.**
> `participants` 에는 **소유자 식별 컬럼이 없고**(스키마 전체 FK 3개 중 auth 연결은
> stats/history 뿐), `participants.id` 는 `p_<Date.now()>` 로 신원과 무관하다.
> 게다가 상관을 만들 수 있던 `user_game_stats.display_name` / `user_game_history.room_id` 는
> 계정 삭제 시 **CASCADE 로 함께 사라진다** — 즉 삭제 후에는 대상 행을 찾을 방법이 없다.
> B·C 는 삭제 **이전 단계**로 옮겨야 하며, 닉네임 문자열 매칭에 의존하면
> **동명이인·게스트의 행을 잘못 고칠 수 있다**. RLS 는 UPDATE/DELETE 를 24시간 이내 행으로
> 제한하므로 그보다 오래된 행은 service_role 이라야 손댈 수 있다.
> 또한 잔존 닉네임은 `jp_participants_select using (true)` 로 **anon 키만으로 조회 가능**하다.
> 전체 근거: [`JP_BL_022_TECHNICAL_TRACEABILITY.md`](JP_BL_022_TECHNICAL_TRACEABILITY.md)

⚠️ **CEO 지시로 어느 것도 구현하지 않았다.** B·C 는 CORE 게임 동작에 영향을 주므로 승인 후 별도 슬라이스가 필요하다.

**현재 사용자 대면 문구는 "모든 게임 기록이 완전히 삭제된다"고 주장하지 않는다** — JP-BL-022 가 해소될 때까지 그렇게 주장하지 않는다.

---

## LINE 진입 아키텍처 (JP-BL-008 / JP-BL-009)

| # | 질문 |
|---|---|
| 1 | **신원 권위** — LIFF `getProfile()` / ID Token / 백엔드 검증 중 무엇인가 |
| 2 | **LINE user ID 저장** 가부, 보관 위치·기간·처리위탁 고지 요건 |
| 3 | **게스트 병행** — MINI App 내 LINE 인증 없는 게스트 플레이 허용 여부 |
| 4 | **연동 해제(unlink)/삭제** 시 게임 데이터 처리 |
| 5 | **채널 구조** — LINE Login 채널과 MINI App 채널 분리 여부 |
| 6 | **운영자 귀속** — 채널·심사 제출 주체 |
| 7 | **Official Account** V1 범위 포함 여부 (현재 P2) |

엔지니어링 준비 상태: 어댑터 경계 설계 완료(`docs/JP_LIFF_ADAPTER_BOUNDARY_2026-09-02.md`).
교체 지점 3곳(진입·신원·공유)으로 좁혀져 있으며 그 아래 계층은 무변경이다.

---

## 지원 / 운영 (Sprint JP-02B 신규)

| # | 질문 |
|---|---|
| 1 | **문의 창구**를 무엇으로 하는가 (이메일/웹폼/LINE OA) — 현재 `JP_SUPPORT_CONFIG` 전 필드 null |
| 2 | 개인정보 문의 창구를 일반 문의와 **분리**해야 하는가 |
| 3 | **응답 시간**을 표시해도 되는가 (현재 어떤 약속도 하지 않는다) |
| 4 | 지원 문의 시 수집 가능한 정보의 범위 — 현재는 진단 참조번호(난수)만 |

---

## OPERATOR_DEPENDENCY (운영자 확정 전까지 진행 불가)

- LINE Developers 계정 및 채널 발급 (Login / LIFF / MINI App / Official Account)
- LINE MINI App 심사 제출 주체
- 일본 사업자 등록 · 특定商取引法 표기 주체
- 개인정보 처리자 / 처리위탁 명시 주체
- 지원 창구(이메일·웹폼) 개설 주체

엔지니어링 준비 상태: `JP_OPERATOR_CONFIG` 가 교체 가능·null-safe 구조로 존재한다.
**개인 신원은 커밋돼 있지 않다.** 확정되면 배포 설정으로 주입한다.
`WOORIMARU_IP_OWNER` 는 운영자와 독립이며 변하지 않는다.
