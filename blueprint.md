# 프로젝트 블루프린트: QR 가위바위보 파티 게임

## 프로젝트 개요
이 프로젝트는 친구들과 QR 코드를 통해 실시간으로 가위바위보 게임을 즐길 수 있는 웹 애플리케이션입니다. 호스트가 방을 생성하면 참가자들이 QR 코드로 접속하여 함께 게임을 진행하고 벌칙을 정할 수 있습니다.

## 현재 구현 상태 (v1.0.0 - 프로토타입)
- **UI/UX**: 모바일 퍼스트 디자인, 가이드라인에 따른 깔끔한 레이아웃 및 애니메이션 적용.
- **주요 화면**: 홈, 호스트 방(QR 생성), 참가자 참여, 벌칙 설정, 게임 진행(타이머), 결과 확인, 통계.
- **기능**:
    - `localStorage`를 이용한 로컬 상태 관리.
    - 호스트/참가자 역할 구분 UI 구현.
    - 가위바위보 로직 및 결과 판정 구현.
    - 벌칙 이력 관리 및 랜덤 선택 기능.
- **제약 사항**: 현재는 단일 브라우저 내에서만 동작하며, 다른 기기 간 실시간 동기화는 백엔드 연동이 필요함.

## 향후 계획 및 단계별 목표

### 1단계: 백엔드 연동 준비 및 검사 (현재 단계)
- 현재 HTML/JS 코드의 정상 동작 여부 최종 확인.
- Supabase(또는 Firebase) 연동을 위한 라이브러리 추가 및 초기화 설정.
- 실시간 데이터 동기화를 위한 데이터 스키마 설계.

### 2단계: 실시간 멀티플레이어 구현 (진행 중)
- **데이터베이스 스키마 설계**:
    - `rooms`: `id`(방 코드), `penalty`(벌칙), `round`(현재 라운드), `status`(게임 상태), `created_at`
    - `participants`: `id`, `room_id`, `name`(닉네임), `choice`(가위/바위/보), `wins`, `losses`, `draws`, `penalties`, `is_host`, `last_active_at`
- **Realtime 서비스 연동**:
    - `supabase.channel().on('postgres_changes', ...)`를 사용하여 데이터 변경 시 UI 즉각 업데이트.
- **로직 전환**:
    - `createRoom`: Supabase `rooms` 테이블에 레코드 삽입.
    - `joinRoom`: `participants` 테이블에 레코드 삽입 및 해당 방 구독 시작.
    - `selectChoice`: 본인의 `choice` 컬럼 업데이트.

### 3단계: 배포 및 테스트
- Supabase 등을 통해 애플리케이션 배포.
- 실제 모바일 기기 여러 대를 사용한 필드 테스트 및 버그 수정.

## 현재 작업 계획 (Supabase 실시간 연동)
1.  **SQL 스크립트 작성**: Supabase 대시보드에서 실행할 테이블 생성 스크립트 제공.
2.  **클라이언트 로직 수정**: `index.html` 내 JS 함수들을 Supabase API 호출 방식으로 전면 개편.
3.  **실시간 구독 설정**: 방의 상태 변화와 참가자 리스트 변화를 감지하는 Listener 추가.
