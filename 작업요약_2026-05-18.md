# 가위바위보 게임 버그 수정 작업 요약 (2026-05-18)

**브랜치:** `claude/fix-game-ready-button-bl7zf`  
**수정 파일:** `index.html` (단일 파일 앱)

---

## 커밋 목록 (시간순)

| 커밋 | 내용 |
|------|------|
| `94a3344` | 게임 준비 버튼 대기↔준비 깜빡임 버그 수정 |
| `9b7ff2e` | 재게임 선택 시 게임준비 버튼 미표시 수정 |
| `4a8cbda` | 참가자 단말기 TTS 미재생 수정 |
| `145e954` | 참가자 화면 간 카운트다운 시차 보정 |
| `fc00595` | round=1 경쟁 조건으로 인한 게임준비 버튼 오탐 수정 |
| `329bd54` | fetchParticipants stale choice 정규화 |
| `065af4c` | screenParticipantWait 고착 안전망 추가 |
| `4e3e1f3` | 대기실 1분 무조작 시 자동 로그아웃 기능 추가 |
| `9ce864b` | 드롭된 참가자 ready/reinviting 상태에서 정리 |
| `355a656` | 신규 참가자 미표시 + 게임준비 버튼 깜빡임 수정 |

---

## 수정된 버그 상세

### 1. 게임 준비 버튼 대기↔준비 깜빡임 (`94a3344`)

**증상:** 한판더 후 3초마다 준비 버튼이 리셋됨  
**원인:** `handleRoomUpdate()`의 `is_ready = false` 초기화가 폴링(3초)마다 실행  
**수정:** `is_ready` 리셋을 `oldStatus → "ready"` 전환 시 한 번만 실행

```
index.html ~1366–1373
```

---

### 2. 재게임 선택 시 게임준비 버튼 미표시 (`9b7ff2e`)

**증상:** 한판더 후 참가자 화면에 게임준비 버튼 안 나옴  
**원인 (복합):**
- `invitePopup`이 `hideAllScreens()`에 포함되지 않아 `screenReady` 위를 덮음
- `acceptInvite()`가 이미 `status="ready"`인 상황에서도 `screenParticipantWait`로 이동

**수정:**
- `status → "ready"` 전환 시 `invitePopup` 자동 닫기
- `acceptInvite()`에서 현재 상태 확인 후 분기 처리

```
index.html ~1372 (invitePopup 닫기)
index.html ~2548–2560 (acceptInvite 분기)
```

---

### 3. 참가자 단말기 TTS 미재생 (`4a8cbda`)

**증상:** 게임 시작 카운트다운 TTS가 참가자 기기에서만 무음  
**원인:** 모바일 브라우저의 사용자 제스처 요구 정책. WebSocket 이벤트 경로는 제스처 컨텍스트 없음  
**수정:**
- `markReady()` 클릭 시점에 빈 발화로 `speechSynthesis` 미리 잠금 해제
- `speak()`에서 `speaking` 상태일 때도 `resume()` 호출

```
index.html ~2878–2882 (markReady TTS 잠금 해제)
index.html ~3000 내외 (speak 함수 resume 보완)
```

---

### 4. 참가자 화면 간 카운트다운 시차 (`145e954`)

**증상:** 기기마다 카운트다운 시작 시점이 달라 화면이 따로 놈  
**원인:**
- 기기별 네트워크 지연으로 Supabase 이벤트 수신 시각 상이
- TTS `await`으로 기기별 음성 속도 차이가 시차 증폭

**수정:**
- `payload.commit_timestamp`(DB 커밋 UTC)를 `handleRoomUpdate`에 전달
- `elapsedMs = Date.now() - commitTimestamp`로 경과 시간 계산 후 카운트다운 건너뜀
- 고정 구간: 1단계 1500ms + 2단계 1800ms = 3300ms
- TTS는 `await` 없이 병렬 실행 (타이밍 고정)

```
index.html ~1299–1303 (commitTime 추출)
index.html ~1405–1406 (elapsedMs 전달)
index.html ~2035–2087 (runCountdown 시차 보정 로직)
```

---

### 5. round=1 경쟁 조건으로 게임준비 버튼 오탐 (`fc00595`)

**증상:** 재게임 수락 후 "게임 준비" 대신 비활성 "게임 대기" 표시  
**원인:** 폴링이 `resetGameKeepRoom()` DB 쓰기 사이에 끼어 `__loser__` 마커 포함 구 스냅샷 읽음  
**수정:**
- `isSafeParticipant()` / `isConfirmedLoser()`: `state.round === 1`이면 `p.choice` fallback 무시
- `handleRoomUpdate` "reinviting" 분기: `oldStatus !== "ready"`일 때만 invitePopup 표시

```
index.html ~1230–1246 (isSafeParticipant/isConfirmedLoser round=1 처리)
index.html ~1424–1427 (reinviting 역행 방지)
```

---

### 6. fetchParticipants stale choice 정규화 (`329bd54`)

**증상:** 첫 게임 후 재게임 시 비활성 버튼 표시  
**원인:** `fetchParticipants()`가 구 스냅샷으로 `state.participants`를 덮어써 stale `is_ready=true` 잔류  
**수정:** `fetchParticipants()` 완료 후 `status="ready" && round=1`일 때 `choice !== null`인 항목 강제 정규화

```
index.html ~1472–1478
```

---

### 7. screenParticipantWait 고착 안전망 (`065af4c`)

**증상:** 재게임 수락 후 대기 화면에 계속 머묾  
**원인:** `handleRoomUpdate`의 화면 전환이 `oldStatus !== state.status` 블록 안에만 존재. 동일 status 반복 호출 시 전환 건너뜀  
**수정:** 전환 블록 외부에 추가 안전망 — `ready` 상태 + 참가자 역할 + `screenParticipantWait` visible이면 `showReadyScreen()` 강제 호출

```
index.html ~1431–1439 (handleRoomUpdate 이후)
index.html ~1482–1489 (fetchParticipants 이후)
```

---

### 8. 대기실 자동 로그아웃 기능 (`4e3e1f3`)

**배경:** 자리를 비운 참가자가 목록에 남아 전원 준비 조건 미충족 문제 예방  
**동작:**
- `screenParticipantWait` 진입 시 60초 타이머 시작
- 만료 시 5초 카운트다운 팝업 표시 (`idleLogoutPopup`)
- "여기 있어요!" 버튼 클릭 시 취소 + 타이머 재시작
- 5초 내 미응답 → `_doLeaveRoom()` 자동 퇴장
- 터치/클릭 이벤트로 타이머 리셋 (카운트다운 중 무시)
- `showScreen()` 호출 시 타이머 자동 초기화

```
index.html ~1048–1055 (팝업 HTML)
index.html ~1563–1571 (showScreen 연동)
index.html ~3254–3298 (자동 로그아웃 로직 전체)
```

---

### 9. 드롭된 참가자 ready/reinviting 상태에서 미제거 (`9ce864b`)

**증상:** 재게임 준비 중 오프라인 참가자가 목록에 계속 표시돼 전원 준비 불가  
**원인:** `cleanupDroppedParticipants()`가 `"waiting"` 상태에서만 실행  
**수정:**
- 허용 상태 확장: `["waiting", "ready", "reinviting"]`
- Grace period 차등: `waiting` = 5분, `ready/reinviting` = 2분
- `playing/result` 중에는 여전히 실행 안 함

```
index.html ~1250–1283
```

---

### 10. 신규 참가자 미표시 + 게임준비 버튼 깜빡임 (`355a656`)

**증상:**
1. QR 스캔 후 참가자가 호스트 화면에 즉시 안 나타남
2. "게임 준비" 누른 후 버튼이 "✋ 게임 준비"로 되돌아가는 깜빡임

**원인:**
1. `postgres_changes` INSERT 콜백의 `fetchParticipants()` 호출이 `fetchParticipantsSeq`에 의해 드롭
2. `markReady()` DB 성공 후 로컬 `state.participants[me].is_ready` 미갱신 → WebSocket 이벤트 시 stale 상태로 renderAll

**수정:**
- INSERT 이벤트: `payload.new`로 즉시 `state.participants`에 추가 + `renderAll()` (seq 우회)
- `markReady()` 성공 후 즉시 `me.is_ready = true; renderAll()`

```
index.html ~1305–1312 (INSERT 즉시 반영)
index.html ~2890–2893 (markReady 로컬 상태 갱신)
```

---

## 핵심 설계 원칙 (재사용 시 참고)

1. **폴링과 WebSocket의 경쟁 조건**: `fetchParticipantsSeq` 카운터로 오래된 응답 드롭. INSERT는 seq 우회해 즉시 반영.

2. **로컬 상태 vs DB 상태 우선순위**: DB 성공 직후 로컬 상태 즉시 반영 → WebSocket 이벤트의 stale 덮어쓰기 방지.

3. **상태 전환 조건**: `oldStatus !== state.status` 블록 + 별도 안전망(idempotent `showReadyScreen()`)의 이중 구조.

4. **모바일 TTS**: 사용자 제스처 컨텍스트(버튼 클릭)에서 미리 `speechSynthesis.speak('')`로 잠금 해제 필수.

5. **카운트다운 동기화**: `commit_timestamp` 기준 경과 시간 계산 + TTS는 `await` 없이 병렬 실행.

---

## 현재 브랜치 상태

- 브랜치: `claude/fix-game-ready-button-bl7zf`
- 최신 커밋: `1d1ff51` (CLAUDE.md 문서 추가)
- 원격 동기: `origin/claude/fix-game-ready-button-bl7zf` ✅
- 미완료 작업: 없음 (모든 수정 완료 및 커밋됨)
